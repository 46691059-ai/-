package cn.gov.enterprise.modules.workflow.domain.role;

/** Runtime execution state, independent from approval persistence and Canary enablement. */
public enum RoleRuntimeActivationState {
    DISABLED, ACTIVATED
}
