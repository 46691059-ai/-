# Workflow联调资源交付请求报告

## 1. 报告信息

| 项目 | 内容 |
|---|---|
| Sprint | Sprint 2-3.7.4 |
| 检查日期 | 2026-08-10（Asia/Shanghai） |
| 用途 | 建立Sprint 2-3.7真实Workflow联调资源准入清单 |
| 检查范围 | 服务、安全、流程、账号、数据库资源 |
| 当前结论 | **RESOURCE_DELIVERY_PENDING / NOT_READY** |

本报告是资源交付请求单，不执行业务联调。检查期间未修改业务代码，未修改V2.4.0—V2.4.9 Migration，未创建Workflow数据库，未启动Outbox Worker，也未输出、保存或推断任何密钥值。

历史临时MySQL验收、源码默认值、契约示例和本机未知服务监听均不作为当前联调环境已经具备的证据。

## 2. 已具备资源

### 2.1 当前联调运行资源

当前没有可通过准入的联调运行资源。

| 资源 | 当前事实 | 是否可用于联调 |
|---|---|---|
| Investment地址 | 未交付；默认`127.0.0.1:8080`无监听 | 否 |
| Workflow地址 | 未交付；默认`127.0.0.1:8090`无监听 | 否 |
| Redis地址 | 未交付；默认`127.0.0.1:6379`无监听 | 否 |
| MySQL地址 | 应用JDBC未交付；默认`127.0.0.1:3306`无监听 | 否 |
| 本机`127.0.0.1:3307`监听 | 有`mysqld`监听，但无JDBC、认证、版本、Schema和Flyway证明 | 否 |

### 2.2 已具备工程资产

以下资产已存在于仓库，但它们不等同于当前联调环境资源：

| 工程资产 | 状态 | 用途 |
|---|---|---|
| Workflow HTTP Client/Adapter | 已存在 | 流程启动、实例、任务、事件和撤回调用 |
| 回调Token及HMAC校验能力 | 已存在 | 回调认证、签名、时间戳和Nonce检查 |
| Workflow Inbox/Outbox可靠性结构 | 已存在 | 消息持久化、去重、重试、死信和重放审计 |
| Workflow与Investment接口契约 | 已冻结 | 约束请求、回调、状态和安全边界 |
| V2.4.9 Migration资产 | 完整性通过 | 本地SQL SHA-256与Migration Inventory一致 |
| Flyway环境检查脚本 | 已存在 | 目标库版本、history、validate和指纹检查 |
| Schema指纹SQL | 已存在 | 对目标MySQL结构进行只读指纹计算 |

当前终端没有可用的Java、Maven和MySQL客户端命令，因此这些工程资产尚未在当前环境完成构建、启动或数据库准入验证。

## 3. 缺失资源

### 3.1 服务资源

| 编号 | 待交付资源 | 交付方 | 交付形式 | 准入证据 |
|---|---|---|---|---|
| SVC-01 | Investment非生产地址 | 平台运维/Investment团队 | DNS或受控URL，不含凭证 | TCP/HTTPS可达，健康检查成功，版本可识别 |
| SVC-02 | Workflow非生产地址 | Workflow团队/平台运维 | `WORKFLOW_BASE_URL`运行时配置 | 健康检查成功，接口契约版本兼容 |
| SVC-03 | Redis非生产地址 | 中间件/平台运维 | 主机、端口、TLS及认证引用 | 连接、读写、TTL、Nonce及锁能力通过 |
| SVC-04 | MySQL 8非生产地址 | DBA/平台运维 | JDBC及凭证引用 | 连接成功、目标Schema正确、只读检查通过 |
| SVC-05 | 双向网络资源 | 平台运维/网络安全 | DNS、路由、TLS证书及允许列表 | Investment调用Workflow及Workflow回调Investment均成功 |

地址可以记录在受控环境清单中；地址中不得嵌入用户名、密码、Token或Secret。

### 3.2 安全资源

当前以下配置均未交付：

| 编号 | 环境变量 | 交付方 | 交付要求 | 报告记录规则 |
|---|---|---|---|---|
| SEC-01 | `WORKFLOW_SERVICE_TOKEN` | Workflow团队/安全团队 | 非生产专用、最小权限、可轮换 | 仅记录存在、有效、到期时间是否满足，不记录值 |
| SEC-02 | `WORKFLOW_CALLBACK_TOKEN` | Investment团队/安全团队 | 与服务Token用途隔离 | 仅记录存在和握手结果，不记录值 |
| SEC-03 | `WORKFLOW_HMAC_CURRENT_KEY_ID` | 安全团队 | 稳定Key ID及轮换元数据 | 可记录Key ID存在性，不记录关联Secret |
| SEC-04 | `WORKFLOW_HMAC_CURRENT_SECRET` | 安全团队 | 高强度随机密钥，经密钥设施注入 | 仅记录存在、格式和签名结果，不记录值 |

安全资源不得通过Git、普通`.env`、YAML、镜像、邮件正文、即时消息、报告、截图或应用日志交付。应使用密钥管理系统、CI/CD受保护变量或等价受控注入方式。

还需交付密钥轮换方案、当前/前一Key有效窗口、时钟同步要求、Nonce保留期和紧急吊销联系人。

### 3.3 流程资源

| 编号 | 待交付资源 | 交付方 | 必要内容 | 准入证据 |
|---|---|---|---|---|
| WF-01 | 投资决策真实流程ID | Workflow团队 | 环境内唯一ID及定义键 | Workflow API查询结果 |
| WF-02 | 固定流程版本 | Workflow团队/业务治理方 | 已发布、已启用、禁止自动漂移 | 版本查询及启动前校验 |
| WF-03 | 节点配置 | Workflow团队/业务治理方 | 节点编码、顺序、条件、候选组、超时和授权 | 节点元数据与冻结路线比对通过 |
| WF-04 | 异常路线 | Workflow团队 | 驳回、撤回、终止、异常及补偿行为 | 受控测试结果 |
| WF-05 | 回调订阅 | Workflow团队/Investment团队 | 事件类型、回调地址、契约版本及签名策略 | 签名握手和事件回执成功 |

流程节点应按冻结的决策路线选择业务、财务、法务合规、风险、投资评审、三重一大、党委前置研究、经理层、董事会、股东会/出资人、监管审批及附条件复核节点。禁止把契约示例中的流程版本或节点列表直接当成真实部署证明。

### 3.4 账号资源

| 编号 | 待交付资源 | 交付方 | 必要权限 | 准入证据 |
|---|---|---|---|---|
| IAM-01 | Investment发起人 | IAM/系统管理员 | 登录、组织数据范围、`investment:decision:submit` | 实际登录和权限查询 |
| IAM-02 | Workflow审批人 | Workflow管理员 | 候选、签收、查询、办理指定节点任务 | 实际任务授权查询 |
| IAM-03 | 条件复核人 | Workflow/业务管理员 | 条件复核、关闭或经授权豁免 | 节点任务权限结果 |
| IAM-04 | 联调管理员 | 平台管理员 | 最小排障权限及经审批的`investment:workflow:replay` | RBAC查询及审计开启 |
| IAM-05 | 服务身份 | 安全团队/Workflow团队 | Investment到Workflow最小API权限 | Token认证和授权结果 |
| IAM-06 | 身份映射 | IAM/双方系统团队 | Investment用户、组织与Workflow主体稳定映射 | 映射清单及实际查询一致 |

发起人不得作为同一事项唯一最终审批人；服务身份不得冒充业务办理人。角色、候选组和职责分离必须由实际系统数据证明，不能只引用Migration权限定义。

### 3.5 数据库资源

| 编号 | 待交付资源 | 交付方 | 必要内容 | 准入证据 |
|---|---|---|---|---|
| DB-01 | JDBC配置 | DBA/平台运维 | `DB_URL`、用户名及安全凭证引用 | Investment数据源连接成功 |
| DB-02 | MySQL版本和Schema | DBA | MySQL 8、目标Schema名称和字符集 | 只读版本及Schema查询 |
| DB-03 | Flyway历史 | DBA/发布团队 | `flyway_schema_history`读取权限 | 无失败记录，最终版本V2.4.9 |
| DB-04 | Flyway Validate | 发布团队 | 正式扫描目录和严格校验 | checksum全部一致，不执行自动`repair` |
| DB-05 | V2.4.9结构指纹 | DBA/发布团队 | 在目标库执行指纹SQL | 与批准基准一致 |
| DB-06 | 可靠性结构 | DBA/Investment团队 | V2.4.9表、字段、索引、外键、CHECK及权限 | 结构核查SQL通过 |

历史隔离MySQL环境的V2.4.9验收只说明Migration资产曾通过验证，不说明本次目标数据库已应用或一致。当前目标库必须独立读取Flyway历史并重新计算指纹。

## 4. 联调启动条件

只有以下条件全部通过，才能开始Sprint 2-3.7：

1. SVC-01至SVC-05全部交付并完成实际连通性与健康检查。
2. SEC-01至SEC-04通过安全渠道注入，Token认证和HMAC握手成功，报告中不出现密钥值。
3. WF-01至WF-05由Workflow API和受控测试证明，流程版本被显式锁定。
4. IAM-01至IAM-06完成实际账号、RBAC、候选组、任务权限和职责分离核验。
5. DB-01至DB-06通过，目标Schema最终版本为V2.4.9，严格`validate`和结构指纹一致。
6. Investment使用Java 21构建并实际启动，健康状态正常，MySQL及Redis依赖均为UP。
7. Investment与Workflow双向TLS、Token、HMAC、时间窗口及Nonce防重放验证通过。
8. 联调前保持`WORKFLOW_OUTBOX_WORKER_ENABLED=false`；单次受控直连成功且获得变更窗口批准后，才允许在Sprint 2-3.7中启用。
9. traceId、审计日志、失败停止、死信、重放审批和测试数据清理方案已准备。
10. 所有证据来自当前非生产环境，不得复用历史临时环境结果代替。

## 5. 责任边界

| 责任方 | 负责 | 不负责/禁止 |
|---|---|---|
| Investment团队 | 应用构建与部署、客户端契约、业务状态、快照、绑定、Inbox/Outbox、回调校验及审计 | 不实现Workflow引擎；不直接修改Workflow任务和流程权威数据 |
| Workflow团队 | Workflow服务、流程定义/版本、任务、节点、候选组、审批权威状态及回调发送 | 不修改Investment业务状态、决策快照或业务数据库 |
| 平台运维 | 非生产计算资源、DNS、网络、TLS、服务部署、环境变量注入及可观测性 | 不接收或传播明文密钥；不替业务方确认流程合法性 |
| 安全团队 | Token/HMAC生成、保管、注入、轮换、吊销、允许列表和安全审计要求 | 不在文档或普通配置中交付密钥明文 |
| DBA/发布团队 | MySQL、JDBC安全交付、Flyway history/validate、Schema指纹、备份与变更窗口 | 不创建Workflow数据库；不修改冻结Migration；不自动`repair` |
| Redis/中间件团队 | Redis实例、认证、TLS、可用性、TTL和容量保障 | 不承担业务事件正确性 |
| IAM/业务管理员 | 发起人、审批人、候选组、节点权限、组织映射及职责分离 | 不以服务账号代替业务审批人 |
| 联调负责人 | 汇总当前环境证据、组织准入评审、控制Worker启停和异常停止 | 不以设计文档、历史报告或模拟结果签署READY |

跨系统权威边界保持不变：Investment维护投资决策、快照、条件整改及业务审计；Workflow维护流程实例、节点、任务、办理权限和审批过程。双方通过冻结契约、绑定记录和幂等事件同步，不跨库读写。

## 6. 资源交付结论

当前仅具备仓库内工程资产，真实联调运行资源尚未交付，状态为：

**RESOURCE_DELIVERY_PENDING / NOT_READY**

在服务、安全、流程、账号和数据库资源全部提供并通过当前环境准入前，不启动Outbox Worker，不执行Sprint 2-3.7业务联调，也不发布READY结论。
