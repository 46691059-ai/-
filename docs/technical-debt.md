# 基础平台历史代码治理记录

版本基线：`v1.0.0-foundation`

## 1. 旧Project Layout

位置：`frontend/src/layouts/ProjectLayout.vue`

状态：保留

原因：该布局属于既有项目模块历史代码。本次基础平台冻结不迁移、不删除，避免影响已有业务页面；后续应在项目生命周期专项 Sprint 中确认引用关系后治理。

## 2. HTTP兼容层

位置：`frontend/src/utils/http.ts`

状态：保留

原因：既有项目 API 仍通过兼容导出使用统一 Axios 客户端。本次不强制改写调用方，避免影响已有业务代码；新模块统一使用 `frontend/src/utils/request.ts`。

## 3. 双样式目录

位置：

- `frontend/src/styles`
- `frontend/src/assets/styles`

状态：待治理

原因：两个目录当前均被前端入口使用，直接合并可能改变已有页面样式。后续应先明确全局基础样式与主题变量职责，并在视觉回归测试保护下逐步合并。

## 治理原则

1. 不以删除历史代码作为版本冻结条件。
2. 治理前先建立引用清单和回归测试。
3. 不在无业务确认的情况下迁移目录或批量改写引用。
4. 技术债处理不得绕过权限、安全和构建质量门禁。
