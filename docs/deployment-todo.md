# 基础平台部署待办

版本基线：`v1.0.0-foundation`

本文件只记录后续部署验证事项，不表示当前版本已经完成生产部署或具备 production 标记。

## Docker部署

状态：待验证

待办：在具备 Docker Engine 与 Docker Compose 插件的 Linux 主机执行配置展开、镜像构建、健康检查和容器重启验证。

## Linux环境部署

状态：待验证

待办：核验服务器目录权限、非 root 用户运行、时区、资源限制、时间同步、防火墙和服务自启动策略。

## Nginx

状态：待验证

待办：验证静态资源、Vue Router history 回退、后端反向代理、请求大小限制、安全响应头及访问日志。

## HTTPS

状态：待验证

待办：明确 TLS 终止位置，配置受信任证书、自动续期、安全协议套件、HTTP 跳转 HTTPS 及 HSTS 策略。

## 数据库备份

状态：待验证

待办：执行 MySQL 全量备份、保留周期、校验和、异地存储、恢复演练及恢复时间目标验证。

## Redis持久化

状态：待验证

待办：验证 Redis 7.4 AOF/RDB 策略、密码注入、数据卷权限、重启恢复、内存淘汰和监控告警。
